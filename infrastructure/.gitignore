# Local Terraform working files and state. State can contain resource details
# and must never be committed.
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
out.json
dist/
