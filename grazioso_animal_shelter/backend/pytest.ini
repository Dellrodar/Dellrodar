[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
; Verbose so failures show the exact test and assertion; coverage reported
; per file so drops below the 80% floor are visible immediately.
addopts = -v --cov=app --cov-report=term-missing
